[pytest]
testpaths = tests
# 缓存统一收纳到 .cache/（gitignore 整目录），不再在根目录散落 .pytest_cache
cache_dir = .cache/pytest
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
